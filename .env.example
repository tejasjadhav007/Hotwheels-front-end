# Backend database settings
DB_DRIVER=sqlite
SQLITE_PATH=backend/hotwheels.db

# PostgreSQL credentials (used only when DB_DRIVER=postgresql)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=hotwheels
DB_USER=hotwheels_user
DB_PASSWORD=change_me
